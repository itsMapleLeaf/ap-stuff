[project]
name = "manuals"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = ["pip>=25.2"]

[tool.pyright]
include = ["./template/src/spec"]
extraPaths = ["archipelago"]
